site_name: 🐼🩺 Pandas Vet
docs_dir: docs
plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          options:
            show_source: false
theme:
  name: material
repo_url: https://github.com/cparmet/pandas-vet/
